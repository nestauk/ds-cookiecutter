#!/bin/bash
[ -f .env ] && source .env

# export S3_INPUT_PATH="s3://nesta-ds-projects/<MISSION OR PRACTICE>/<PROJECT NAME>"
{% if cookiecutter.venv_type == 'conda' %}
# Activate the conda environment
eval "$(conda shell.bash activate "{{ cookiecutter.repo_name }}")"
[ "$CONDA_DEFAULT_ENV" = "{{ cookiecutter.repo_name }}" ] || echo "Have you run \`make install\`?"
{% endif %}