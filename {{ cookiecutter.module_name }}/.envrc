#!/bin/bash
# MAKE EDITS TO .ENV INSTEAD
# Uncomment the next line if you have a private UV_INDEX configured locally.
# This prevents private package indices from polluting uv.lock in this project.
# unset UV_INDEX
[ -f .env ] && source .env
